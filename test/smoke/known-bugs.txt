# Известные баги дымового теста — один паттерн на строку, '#' = комментарий.
# Если ВСЕ падения smoke.sh матчат один из паттернов (подстрока в сообщении
# FAIL: ...), скрипт выходит 0: фича ещё в разработке, но регрессий нет.
# Пустой файл = любое падение красное. Формат унаследован из Intermasq.
#
# Пример (раскомментировать, когда баг заведён, но не блокирует релиз):
#   podman: host-gateway not supported on this podman version
